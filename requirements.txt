# NovelForge v2.0 Requirements
# Install: pip install -r requirements.txt

# Core
aiohttp>=3.9.0
httpx>=0.27.0
pydantic>=2.0.0
websockets>=12.0

# MCP Protocol
mcp>=1.0.0

# MemPalace (memory storage)
# Install separately: pip install mempalace
# Or from source if needed

# Crawl4AI (research)
# Run via Docker: docker run -d -p 11235:11235 --shm-size=1g unclecode/crawl4ai:latest
# No Python dependency needed - communicates via HTTP

# Optional: For local model serving
# Use LM Studio, Ollama, or vLLM
